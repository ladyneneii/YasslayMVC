@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@using YasslayMVC.Controllers;
@{
    ViewBag.Title = "Index";
}

                    </ul>
                </div>
            </div>
        </nav>
    </header>
<div class="container">
    <main role="main" class="pb-3">

        <table class="table table-bordered table-striped">
            <tr>
                <th>ConfessID</th>
                <th>UserID</th>
                <th>Relationship</th>
                <th>Message</th>
                <th>Recipient's Last Name</th>
                <th>Recipient's First Name</th>
                <th>Gift</th>
                <th>Status</th>
                <th></th>
            </tr>
            @for (var i = 0; i < Model.Rows.Count; i++)
            {
                <tr>
                    <td>@Model.Rows[i][0]</td>
                    <td>@Model.Rows[i][1]</td>
                    <td>@Model.Rows[i][2]</td>
                    <td>@Model.Rows[i][3]</td>
                    <td>@Model.Rows[i][4]</td>
                    <td>@Model.Rows[i][5]</td>
                    <td>@Model.Rows[i][6]</td>
                    <td>@Model.Rows[i][7]</td>
                    <td>
                        <a href="@Url.Action("Edit_Seller","Confessions",new {@ConfessID=@Model.Rows[i][0],@id=@ViewBag.LinkableId })">Edit</a>
                        <a href="@Url.Action("Delete_Seller","Confessions",new {@ConfessID=@Model.Rows[i][0],@id=@ViewBag.LinkableId })">Delete</a>
                    </td>
                </tr>
            }
        </table>

        <a href="@Url.Action("Index","Gifts",new {@id=@ViewBag.LinkableId })">Back to Gift Shop!</a>

