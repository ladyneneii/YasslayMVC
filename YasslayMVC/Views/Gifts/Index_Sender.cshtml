@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@using YasslayMVC.Controllers;
@{
    ViewBag.Title = "Index";
}

                    </ul>
                </div>
            </div>
        </nav>
    </header>
<div class="container">
    <main role="main" class="pb-3">

        <table class="table table-border">
            <tr>
                <th>GiftID</th>
                <th>GiftName</th>
                <th>Price</th>
                <th>QuantityLeft</th>
            </tr>
            @for (var i = 0; i < Model.Rows.Count; i++)
            {
                if (string.Compare(@Model.Rows[i][4].ToString(), "Available") == 0)
                {
                    <tr>
                        <td>@Model.Rows[i][0]</td>
                        <td>@Model.Rows[i][1]</td>
                        <td>@Model.Rows[i][2]</td>
                        <td>@Model.Rows[i][3]</td>
                    </tr>
                }
            }
        </table>

        <a href="@Url.Action("Create","Confessions",new {@id=@ViewBag.LinkableId })">Write a Confession!</a>
        <a href="@Url.Action("Index","Confessions",new {@id=@ViewBag.LinkableId })">Go Back</a>
